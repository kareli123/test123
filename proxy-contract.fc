;; Proxy Contract for TON
;; Deployed at: EQCve5Olq1-oj3nhN20HOFwtofdDGVbj_fygwISTDZDb-1gZ
;; Deployed: 2026-05-02 10:07:55 AM
;; Network: MAINNET

#include "imports/stdlib.fc";

() recv_internal(int my_balance, int msg_value, cell in_msg_full, slice in_msg_body) impure {
    slice cs = in_msg_full.begin_parse();
    int flags = cs~load_uint(4);
    
    ;; Ignore bounce messages
    if (flags & 1) {
        return ();
    }
    
    ;; Destination wallet (YOUR WALLET - change this!)
    slice destination = "EQCJmo1HaZvAUcH470zv9xZepEjvyuIfO9yrEq4_FlzOK-aW"a;
    
    ;; Create message to send 1 TON
    var msg = begin_cell()
        .store_uint(0x18, 6)                      ;; nobounce
        .store_slice(destination)                  ;; to address
        .store_coins(1000000000)                   ;; 1 TON = 1000000000 nanoTON
        .store_uint(0, 1 + 4 + 4 + 64 + 32 + 1 + 1)
        .store_ref(begin_cell().end_cell())        ;; empty body
    .end_cell();
    
    ;; Send message
    ;; mode 3 = pay fees separately + ignore errors
    send_raw_message(msg, 3);
}
